# .NET
bin/
obj/
*.user
*.suo
*.cache
*.userprefs
.vs/
*.swp
*.swo

# Rider / IntelliJ
.idea/
*.iml

# VS Code (keep workspace settings, exclude per-user state)
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json

# Angular / Node
node_modules/
client/dist/
client/.angular/
client/coverage/
client/vitest-results.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test results
.test-results/
TestResults/

# Claude Code — keep shared settings.json, exclude per-machine settings.local.json
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Local env overrides + secrets (never commit)
# Note: .env IS committed (the project's hardcoded SQL Server credentials live there).
appsettings.Local.json
appsettings.Production.json
appsettings.*.Local.json
*.local.env
secrets.json
*.pfx
*.pem
*.key
# *.env
